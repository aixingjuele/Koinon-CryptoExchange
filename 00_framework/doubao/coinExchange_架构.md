┌─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
│                                                              C 端 客 户 层                                                            │
│   ┌──────────────────────┐  ┌──────────────────────┐  ┌──────────────────────┐  ┌──────────────────────┐  ┌──────────────────────┐    │
│   │  APP / 桌面客户端      │  │      H5 触屏          │  │     PC Web 交易端      │  │   程序化 API 用户      │  │   管理后台运营端       │    │
│   └──────────┬───────────┘  └──────────┬───────────┘  └──────────┬───────────┘  └──────────┬───────────┘  └──────────┬───────────┘    │
│              │                         │                         │                         │                         │                  │
└──────────────┼─────────────────────────┼─────────────────────────┼─────────────────────────┼─────────────────────────┼──────────────────┘
               │                         │                         │                         │                         │
               ▼                         ▼                         ▼                         ▼                         ▼
┌─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
│                                                       网 关 / 负 载 均 衡 层                                                           │
│               Nginx 入口 / API Gateway (鉴权 + 限流 + 签名校验 + 防刷)                LVS (长连接四层负载)                               │
│                        │                                                                 │                                            │
└────────────────────────┼─────────────────────────────────────────────────────────────────┼────────────────────────────────────────────┘
                         │                                                                 │
                         ▼                                                                 ▼
┌─────────────────────────────────────────────────────────┐  ┌─────────────────────────────────────────────────────────────────────────┐
│                   EXCHANGE-API 模块（下单服务）         │  │                 Market Data Push / Netty & WebSocket Server             │
│              (RESTful 下单接口 + 撤单 + 查委托)          │  │            (Spring STOMP WebSocket + Hawk Netty 双协议)                │
│  ┌──────────────────────────────────────────────────┐  │  │  ┌─────────────────────────────────────────────────────────────────┐  │
│  │ OrderController.addOrder() 下单入口                │  │  │  │  NettyCacheUtils (内存映射表):                                    │  │
│  │  ① 参数/余额校验  ② MemberWalletDao.freezeBalance │  │  │  │    topic → Set<Channel>  按订阅分组                                │  │
│  │     (条件原子UPDATE：balance>=x → 可用→冻结)       │  │  │  │    公共组：  "BTC/USDT"     → 所有看盘连接                          │  │
│  │  ③ ExchangeOrder 入库(TRADING 状态+唯一键幂等)    │  │  │  │    个人组：  "BTC/USDT-10086" → 只有本人连接                         │  │
│  │  ④ 发 Kafka 消息 ⚠️ 隐患: 无 key 同 symbol 不保证 │  │  │  │  STOMP: convertAndSend("/topic/market/.../{symbol}/{uid}")         │  │
│  │     同一分区（建议 send(topic, symbol, payload)）   │  │  │  └─────────────────────────────────────────────────────────────────┘  │
│  │     exchange-order / exchange-order-cancel        │  │  │                                                             │       │
│  └──────────────────────────┬───────────────────────┘  │  │  ┌──────────────────────────────────────────────────────────────┐     │
└─────────────────────────────┼──────────────────────────┘  │  │  推送时:                                                        │     │
                              │ Kafka 异步削峰解耦            │  │     公共行情 topic = symbol → 群发组内所有连接                  │     │
                              ▼                             │  │     个人订单 topic = symbol+'-'+uid → 只有本人连接命中          │     │
┌──────────────────────────────────────────────────────────┐  │  │     多节点部署需 Redis Pub/Sub 跨节点广播                       │     │
│                       KAFKA 消息总线 (CQRS 解耦核心)       │  │  └──────────────────────────────────────────────────────────────┘     │
│  Topic:           exchange-order    (下单入口 → 撮合消费)   │  └────────────────────────────────────▲────────────────────────────────┘
│                   exchange-order-cancel (撤单 → 撮合消费)   │                                       │
│                   exchange-trade        (撮合 → 清算消费)   │  ┌────────────────────────────────────┴────────────────────────────────┐
│                   exchange-order-completed (订单完成通知)   │  │                       Market 模块 (清算行情中心)                      │
│                   exchange-trade-plate  (盘口变化推送)      │  │   4 个 @KafkaListener 消费撮合产出的 4 类消息                        │
│                   exchange-order-cancel-success (撤单成功)  │  │ ┌─────────────┐ ┌───────────────┐ ┌────────────┐ ┌─────────────┐  │
└─────────────────────┬──────────────────────────────────────┘  │ │ handleTrade() │ │ orderCompleted() │ │handlePlate() │ │handleCanceled │ │
                      │ KafkaListener concurrency=9 分区串行消费 │ │ (线程池30~100) │ │   (同步低频)      │ │ 进推送缓冲   │ │   (同步低频)   │ │
                      ▼                                          │ └──────┬──────┘ └───────┬───────┘ └──────┬─────┘ └──────┬──────┘  │
┌──────────────────────────────────────────────────────────────┐ │        │                 │                 │               │         │
│                  Exchange 模块 (撮合引擎，内存撮合核心)        │ │        │                 │                 │               │         │
│   CoinTraderFactory:  一个 symbol → 一个 CoinTrader 实例       │ │        │                 │                 │               │         │
│   (按交易对分片 + Kafka symbol分区串行 = 天然无锁撮合)          │ │        │① DB 清算分支     │② 行情聚合分支    │③ 盘口推送     │④ 撤单解冻    │
│  ┌──────────────────────────────────────────────────────────┐│ │        ▼                 ▼                 ▼               ▼         │
│  │ @KafkaListener onOrderSubmitted/onOrderCanceled          ││ │  ┌──────────────────────────────────────────────────────────────┐  │
│  │ 线程池并行9组消费， symbol同分区串行                       ││ │  │ ExchangeOrderService.processExchangeTrade() [资金一致性核心]    │  │
│  └────────────────────────────┬─────────────────────────────┘│ │  │  @Transactional + SELECT...FOR UPDATE 钱包/订单 行锁串行        │  │
│                               ▼                              │ │  │  1. 买入方：BTC increaseBalance + USDT decreaseFrozen（扣冻）    │  │
│  ┌──────────────────────────────────────────────────────────┐│ │  │  2. 卖出方：USDT increaseBalance + BTC decreaseFrozen（扣冻）   │  │
│  │ CoinTrader.trade(newOrder) 每次撮合入口                   ││ │  │  3. 手续费：maker/taker 差异化 + 推荐人返佣                    │  │
│  │  对手方路由→先吃限价簿再吃市价队列                        ││ │  │  4. 流水记录 + 订单 tradedAmount/turnover 累加                  │  │
│  └───────┬──────────────────────┬───────────────────────────┘│ │  │  ⚠️ 隐患：processOrder() 吞异常catch不rethrow → 事务不回滚!       │  │
│          │                      ▼                            │ │  └──────────────────────────────────────────────────────────────┘  │
│          │         ┌──────────────────────────────┐          │ │                                                                   │
│          │         │ synchronized(lpList) 纯内存  │          │ │  ┌──────────────────────────────────────────────────────────────┐  │
│          │         │ TreeMap<价格, MergeOrder>    │          │ │  │ CoinProcessor (每 symbol 1 个 synchronized(currentKLine) 串行) │  │
│          │         │ matchLimitPriceWithLPList()  │          │ │  │  ① processTrade() 增量聚合 1min currentKLine (O=首价,H=max,     │  │
│          │         │  逐档遍历+FIFO同价优先       │          │ │  │    L=min,C=最新价, V=累加量, A=累加额)                          │  │
│          │         │  dealPrice = maker价（市价例外）        │ │  │  ② handleThumb() 今日 24h 聚合 CoinThumb                        │  │
│          │         └──────────────┬───────────────┘          │ │  │  ③ handleTradeStorage() 观察者分发 MarketHandler 列表            │  │
│          │                        │                          │ │  └────────────┬──────────────────────┬────────────────────────┘  │
│          │               ┌────────┴──────────┐               │ │               │                      │                           │
│          │               ▼                   ▼               │ │               ▼                      ▼                           │
│          │  完全成交 tradeCompleted()    剩余量挂簿           │ │  ┌────────────────────┐  ┌────────────────────────┐               │
│          │  completedOrders 收集          addLimitPriceOrder │ │  │  MongoMarketHandler  │  │  WebSocket/Netty Handler │              │
│          │               │                   │               │ │  │ (成交/K线 Mongo落库)  │  │   (实时推送行情给连接)     │              │
│          └───────────────┴───────────────────┴───────────────┘ │  │ exchange_trade_SYMBOL │  │                             │              │
│  ⚠️ 锁外发 Kafka（避免IO阻塞锁 → 减少撤单等锁时间）            │ │  │ exchange_kline_SYMBOL_1min │  │                             │              │
│    ① exchange-trade        → 撮合成交明细 list                 │ │  └────────────────────┘  └────────────────────────┘               │
│    ② exchange-order-completed → 完全成交订单 list              │ │                                                                   │
│    ③ exchange-trade-plate  → TradePlate 全量盘口 (⚠️建议增量diff)│ │  ┌──────────────────────────────────────────────────────────────┐  │
│    ④ 异常兜底发 exchange-order-cancel-success 退冻结          │ │  │ KLineGeneratorJob @Scheduled cron 整点切K线                    │  │
└───────────────────────────────────────────────────────────────┘  │  │  cron(0 * * * * *) 每分钟 → autoGenerate() 切 1min K线         │  │
                                                                   │  │     无成交: 用上一笔close填充防断档                             │  │
┌──────────────────────────────────────────────────────────────────┘  │  │  minute%5/10/15/30==0 → generateKLine(N, MINUTE) 离线聚合     │  │
│                                                                     │  │  小时/日级 cron → generateKLine(1, HOUR/DAY)                    │  │
│     存 储 层（持久化 + 状态机兜底）                                   │  │  周/月K线: 查已有的日线集合做二次聚合 (效率远>扫成交明细)          │  │
│  ┌───────────────┐  ┌───────────────┐  ┌─────────────────────────┐│  │  generateKLine 内部: processTrade() 重放 OHLCV 聚合            │  │
│  │    MySQL       │  │    MongoDB    │  │   Redis 缓存/分布式锁     ││  └──────────────────────────────────────────────────────────────┘  │
│  │  (强事务一致性) │  │ (行情写多读多) │  │  - 用户信息缓存            │└──────────────────────────────────────────────────────────────────┘
│  │  • member_wallet│  │  • exchange_trade_SYMBOL │  • 盘口/缩略缓存          │
│  │    4 原子操作   │  │  • exchange_kline_SYMBOL_period │  • 分布式ID/限流         │
│  │   incr/decr    │  │  - CoinThumb 24h行情快照 │  • Kafka 重试持久化队列    │
│  │   freeze/thaw  │  └───────────────┘  └─────────────────────────┘
│  │    (WHERE>=x 条件原子更新=DB行锁CAS)  │
│  │  • exchange_order 状态机:             │  ┌─────────────────────────────────────────────────────────┐
│  │     TRADING → COMPLETED/CANCELED      │  │  🔒 资 金 一 致 性 四 层 兜 底（面试亮点）                   │
│  │     (UPDATE WHERE status=TRADING 幂等)│  │  ① 发送端: KafkaTemplate.addCallback 失败→Redis ZSET持久化   │
│  │  • exchange_operation_log 资金流水     │  │     重试队列，指数退避重试 + 死信告警                      │
│  │  • exchange_order_detail 成交明细      │  │  ② MatchLog 撮合交易日志(WAL): 撮合结果先写 RocksDB/Kafka  │
│  └───────────────┘                         │     持久化 topic，任何下游丢消息都能按 sequence 拉回         │
│                                            │  ③ 定时对账任务：每分钟扫 status=TRADING 且 5min 未动订单    │
│                                            │     查撮合内存+成交明细 补清算/补撤单 + 三方余额对账        │
│                                            │  ④ 下游幂等：成交唯一索引 + 订单状态机WHERE条件UPDATE        │
│                                            └─────────────────────────────────────────────────────────┘